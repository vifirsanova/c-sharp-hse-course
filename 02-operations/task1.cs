/*
Точка имеет координаты P(2.5; 3.0). Её перемещают на вектор

⃗v = (1.5; −2.0).

Вычислите координаты новой точки.
Для вычисления новых координат используются правила:
x_новое = x + dx, y_новое = y + dy.
*/

using System;

class Program
{
    static void Main(string[] args)
    {
        // Initial point coordinates
        // Translation vector coordinates
        // Calculate the new point coordinates
        Console.WriteLine($"New point: ({newX}, {newY})");
    }
}
