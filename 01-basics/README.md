Примеры кода к занятию «Введение в C#».
Каждый файл — отдельная программа. Чтобы запустить:

```
dotnet new console -n Demo
cd Demo
cp ../HelloWorldFull.cs Program.cs // имя файла с программой для запуска
dotnet run
```
